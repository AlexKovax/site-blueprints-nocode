---
import Layout from '../../layouts/Layout.astro';
import data from '../../data/blueprints.json' with { type: 'json' };

const { course, topics } = data;
const allLessons = data.all_lessons;
---

<Layout title="Le cours — Blueprints" description="Accédez gratuitement à toutes les leçons du cours Blueprints.">
  <div class="container page-header">
    <h1>Blueprint&nbsp;: Les fondations d'une agence no-code</h1>
    <p class="page-header__desc">
      Découvrez les coulisses de la création d'agences no-code à travers une série d'interviews approfondies avec 10 fondateurs d'agences qui partagent ouvertement leurs succès, leurs échecs et leurs apprentissages. Cette formation gratuite, fruit de 4 ans d'observations et d'échanges dans l'écosystème no-code, vous offre une vision réaliste et complète des enjeux.
    </p>
    <div class="course-stats">
      <span><strong>{topics.length}</strong> chapitres</span>
      <span><strong>{allLessons.length}</strong> leçons</span>
      <span><strong>10</strong> interviews</span>
      <span><strong>~15h</strong> de contenu</span>
    </div>
  </div>

  <div class="container curriculum">
    {topics.map((topic) => (
      <div class="topic">
        <h2 class="topic__title">
          {topic.title}
          <span class="topic__count">{topic.lessons.length} leçon{topic.lessons.length > 1 ? 's' : ''}</span>
        </h2>
        <div class="lessons-list">
          {topic.lessons.map((lesson) => (
            <a href={`/cours/${lesson.slug}/`} class="lesson-card">
              <div class="lesson-card__info">
                <h3 class="lesson-card__title">{lesson.title}</h3>
                {lesson.text && (
                  <p class="lesson-card__excerpt">{lesson.text.substring(0, 120)}{lesson.text.length > 120 ? '…' : ''}</p>
                )}
              </div>
              <div class="lesson-card__meta">
                {lesson.video?.runtime_display && (
                  <span class="lesson-card__duration">{lesson.video.runtime_display}</span>
                )}
                {lesson.video?.source === 'youtube' && <span class="lesson-card__source">YouTube</span>}
                {lesson.video?.source === 'shortcode' && <span class="lesson-card__source">Vidéo</span>}
                {!lesson.has_video && lesson.content_html && <span class="lesson-card__source">Texte</span>}
              </div>
            </a>
          ))}
        </div>
      </div>
    ))}
  </div>
</Layout>

<style>
  .page-header {
    padding: 2.5rem 0;
    border-bottom: 3px solid var(--color-text, #000);
  }

  .page-header h1 {
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 800;
    margin-bottom: 1rem;
  }

  .page-header__desc {
    max-width: 700px;
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .course-stats {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    font-size: 0.9rem;
  }

  .course-stats strong {
    font-size: 1.1rem;
  }

  .curriculum {
    padding: 2rem 0;
  }

  .topic {
    margin-bottom: 2rem;
  }

  .topic__title {
    font-size: 1.35rem;
    font-weight: 700;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--color-text, #000);
    margin-bottom: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .topic__count {
    font-size: 0.875rem;
    font-weight: 600;
    color: #666;
  }

  .lessons-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .lesson-card {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 1rem;
    padding: 0.875rem 1rem;
    border: 2px solid var(--color-text, #000);
    text-decoration: none;
    color: var(--color-text, #000);
    transition: all 0.1s;
  }

  .lesson-card:hover {
    transform: translate(-2px, -2px);
    box-shadow: 4px 4px 0 var(--color-text, #000);
  }

  .lesson-card__title {
    font-size: 0.95rem;
    font-weight: 600;
    margin: 0 0 0.125rem;
  }

  .lesson-card__excerpt {
    font-size: 0.8rem;
    color: #555;
    margin: 0;
    line-height: 1.4;
  }

  .lesson-card__meta {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.25rem;
    flex-shrink: 0;
    font-size: 0.8rem;
  }

  .lesson-card__duration {
    font-weight: 700;
    font-variant-numeric: tabular-nums;
  }

  .lesson-card__source {
    font-size: 0.7rem;
    color: #555;
    background: rgba(0,0,0,0.05);
    padding: 0.125rem 0.5rem;
  }
</style>